--- a/script.c
+++ b/script.c
@@ -543,16 +543,3 @@
-static const char *script_engine(const char *path) {
-	const char *dot = strrchr(path, '.');
-	if (dot && strcasecmp(dot, ".js") == 0)
-		/*
-		 * The "/e:JScript" argument forces the Windows script host
-		 * to use the JScript engine. This bypasses rogue programs that
-		 * register as handlers for the ".js" file extension but fail
-		 * to run the script.
-		 */
-		return "cscript.exe /e:JScript";
-	return "cscript.exe";
-}
-
 int script_config_tun(struct openconnect_info *vpninfo, const char *reason)
 {
 	wchar_t *script_w;
@@ -578,2 +565,3 @@
-	if (asprintf(&cmd, "%s \"%s\"", script_engine(vpninfo->vpnc_script), vpninfo->vpnc_script) == -1)
-		return 0;
+	/* OpenConnect GUI configures only its protected fixed Rust executable. */
+	if (asprintf(&cmd, "\"%s\"", vpninfo->vpnc_script) == -1)
+		return -ENOMEM;
@@ -602,1 +590,5 @@
-		ret = WaitForSingleObject(pi.hProcess,10000);
+		ret = WaitForSingleObject(pi.hProcess,180000);
+		if (ret == WAIT_TIMEOUT) {
+			TerminateProcess(pi.hProcess, 1);
+			WaitForSingleObject(pi.hProcess, INFINITE);
+		}
@@ -622,1 +614,1 @@
-				     _("Script did not complete within 10 seconds.\n"));
+				     _("Script did not complete within 180 seconds.\n"));
